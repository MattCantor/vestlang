import { describe, it, expect } from "vitest";
import { parse } from 

describe("AND/OR Precedence", () => {
  it("AND tighter than OR in OR B AND C", () => {
    const statement =
      "VEST FROM EVENT X before EVENT A OR BEFORE EVENT B AND BEFORE EVENT C";
    const const rawAst =
  });
});
