---
"@vestlang/vestlang": minor
---

Export `summarizeVerification`, the one-line human summary composed from a `verifyObservations` result, so consumers can share it instead of hand-copying the composition.
